<h1>LP Framer</h1>

<form class="" action="/search" method="post">
  <input type="text" name="query">
  <input type="submit" value="search">
</form>

{{#if image_url}}
  <p>{{title}}</p>
  <img src="{{image_url}}" alt="{{title}}">
{{else}}
  <p>Search for an LP to begin.</p>
{{/if}}
